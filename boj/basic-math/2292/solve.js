const fs = require('fs');
const input = fs.readFileSync('input').toString().trim();

const n = parseInt(input);

// 1~1       1
// 2~7       6
// 8~19      12
// 20~37     18

console.log((n - 1) / 6);

console.log(1 + 6 * (n - 1));

//n ===1 16n

let count = 1;

while (num < count) {
  if (count === 1) {
  }
}
const getResult = (num) => {};
